/** @type {import('next').NextConfig} */
const nextConfig = {
  // The SDK ships ESM with subpath exports — Next.js 15 handles this natively.
  // No transpilePackages or experimental config needed.
};

export default nextConfig;
